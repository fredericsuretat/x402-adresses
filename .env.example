# Ton adresse wallet USDC sur le réseau Base (Coinbase L2)
# Crée un compte sur coinbase.com → Assets → Base → Copy address
WALLET_ADDRESS=0xTON_ADRESSE_ICI

# Prix en USDC par validation (0.001 = $0.001)
PRICE_USDC=0.001
